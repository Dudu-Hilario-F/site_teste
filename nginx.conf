server {
    # Configuração para ouvir em todas as interfaces de rede (0.0.0.0)
    listen       8080 default_server;
    server_name  192.168.1.11;
    
    # Compressão gzip para melhorar performance
    gzip on;
    gzip_types text/plain text/css application/javascript application/json;
    gzip_min_length 1000;
    
    # Configuração de cache para arquivos estáticos
    location ~* \.(html|css|js|jpg|jpeg|png|gif|ico|svg)$ {
        expires 1d;
        add_header Cache-Control "public, max-age=86400";
    }
    
    # Configuração principal
    location / {
        alias   /usr/share/nginx/html;
        index  index.html;
        try_files $uri $uri/ /index.html;
        # Desabilitar restrições de acesso
        allow all;
    }
    
    # Configuração para logs de acesso
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
    
    # Configuração de segurança
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-XSS-Protection "1; mode=block";
    add_header X-Content-Type-Options "nosniff";
    
    # Configuração de erro
    error_page  404              /index.html;
    error_page   500 502 503 504  /index.html;
}